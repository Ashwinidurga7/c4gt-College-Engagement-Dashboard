# Backend base URL (the /api prefix is added by the client)
VITE_API_URL=http://localhost:5000
# true = every module uses local mock data; false = real API (mock-only modules stay mocked)
VITE_USE_MOCK=true
